# build: remove the orphan root .prettierignore (toolchain is oxfmt)

## Motivation or problem

From the 2026-06-25 multi-dimension audit (`docs/audits/multi-dimension-2026-06-25.md`, finding DX-L). The root formatting toolchain is oxfmt (`.oxfmtrc.json`); there is no Prettier dependency or script at the repo root (`prettier` appears only transitively under `website/`). The root `.prettierignore` (listing `.address-context/` / `.address/`) guards a root Prettier run that does not exist — dead config that can mislead a contributor into thinking Prettier governs root formatting.

## Desired behavior

- [ ] Remove the root `.prettierignore`. (If some workflow actually invokes Prettier at root — none found in `.github/` or `AGENTS.md` — confirm and document it instead.)

## Out of scope / non-goals

- Root file only. Does not touch the `website/` Astro toolchain (which has its own Prettier).

## Relevant codec variant

Not relevant (repo meta)

## Affected surface

Internal implementation

## Agent readiness self-check

- [x] Motivation, desired behavior, and affected surface are clear.

## Comments (the triage comment holds the acceptance criteria)

### @smonn

> *This was generated by AI during triage.*

## Triage: build: remove the orphan root .prettierignore (toolchain is oxfmt)

**Category:** enhancement (repo-meta cleanup)
**Label:** `ready-for-agent`

---

### Verification

Confirmed the reported state in the repo checkout:

- `.prettierignore` exists at the repo root and contains exactly two entries: `.address-context/` and `.address/`.
- `.oxfmtrc.json` exists at the repo root — oxfmt is the active root formatting toolchain.
- No `.github/workflows/*.yml` file references `prettier` (grep returned no matches).
- `AGENTS.md` does not reference `prettier`.
- `prettier` does not appear anywhere under `website/`-sibling scripts that would run at root; it is transitively present only within `website/` itself.

The `.prettierignore` is confirmed dead config: it guards a root Prettier run that does not and cannot be triggered by any current CI step or documented agent workflow.

---

### Acceptance criteria

- [ ] The file `.prettierignore` at the repository root is deleted.
- [ ] No other file is created or modified (this change is a single file deletion).
- [ ] CI passes (no formatting, lint, or build step references the removed file).

### Non-goals / Out of scope

- The `website/` Astro toolchain has its own `.prettierignore` and Prettier config — do not touch it.
- No changes to `.oxfmtrc.json` or any other formatting config.
- No changes to `.github/workflows/` files (none are affected).
- No documentation update needed beyond the file removal itself.

---

### Scope and impact summary

Single-file deletion of dead root config. No code paths, no CI workflows, no public API, no `src/` changes. Zero functional impact; removes a potential contributor confusion point about which formatter governs the root.

---

**Chosen label:** `ready-for-agent` — the task is fully specified, unambiguous, requires no design judgment, involves no workflow-file changes, and reduces to deleting one file.

