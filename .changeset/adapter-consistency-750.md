---
"@smonn/ids": minor
---

Add `nullableIdColumnMysql`, `nullableIdColumnSqlite`, `columnType` option on MySQL/SQLite generated columns; fix Express `idQuery` type cast; normalize NestJS `ParseIdPipe` exception body shape; document Kysely `idPlugin` unbranded-map constraint, `readIdColumn` ORM-boundary divergence, and Fastify-vs-Express storage divergence.
